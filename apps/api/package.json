{
  "name": "@workspace/api",
  "type": "module",
  "scripts": {
    "build": "wrangler build",
    "dev": "wrangler dev",
    "deploy:stage": "wrangler deploy --env stage",
    "deploy:production": "wrangler deploy --env production",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings --env stage"
  },
  "dependencies": {
    "hono": "^4.9.7",
    "@workspace/db": "workspace:*",
    "@workspace/identity": "workspace:*",
    "@workspace/linting": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
